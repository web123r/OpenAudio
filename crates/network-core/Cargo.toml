[package]
name = "network-core"
version = "0.1.0"
edition = "2021"
description = "Platform-neutral OpenAudio packet and wire-format primitives."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
socket2 = { version = "0.5", features = ["all"] }
if-addrs = "0.10"
